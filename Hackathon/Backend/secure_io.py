
def read_in(filename : str ) -> list:

    masked_data = []
    with open(filename, 'r') as f:
        for line in f:
            masked_data.append(line)
    return masked_data


def construct_pile(vec_strings) -> str:

def mask_pile
